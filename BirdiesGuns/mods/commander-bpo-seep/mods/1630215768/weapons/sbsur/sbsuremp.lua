dofile("scripts/device_utility.lua")
if active then
	Projectile = "armouredsbsuremp"
	--[[
	reload = FindSpriteComponent(Root, "Reload")
	if reload then
		reload.Sprite = "sbsuremp-reload-armoured"
	end]]
end
