dofile("scripts/device_utility.lua")
if active then
	Projectile = "armouredsbsurhe"
	--[[
	reload = FindSpriteComponent(Root, "Reload")
	if reload then
		reload.Sprite = "sbsurhe-reload-armoured"
	end]]
end
