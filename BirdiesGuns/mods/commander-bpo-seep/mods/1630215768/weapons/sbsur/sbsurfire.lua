dofile("scripts/device_utility.lua")
if active then
	Projectile = "armouredsbsurfire"
	--[[
	reload = FindSpriteComponent(Root, "Reload")
	if reload then
		reload.Sprite = "sbsurfire-reload-armoured"
	end]]
end